//using bootstrap so dependencies get a chance to load up first
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App/>, document.querySelector('#container-root'));